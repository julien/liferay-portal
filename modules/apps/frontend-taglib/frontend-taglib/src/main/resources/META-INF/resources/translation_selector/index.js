import React from 'react';

const TranslationSelector = (props) => {
	return (
		<div>
			Hello there
		</div>
	);
};

export default function (props) {
	debugger;
	return (
		<TranslationSelector {...props} />
	);
}